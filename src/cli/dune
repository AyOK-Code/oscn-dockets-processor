(executable
  (name app)
  (libraries
    libutils
    liboscn

    core_kernel
    lwt
    lwt.unix
  )
  (preprocess (pps
    lwt_ppx
  ))
  (modes exe)
  (ocamlopt_flags -O3 -unbox-closures)
)

(env
  (dev
    (flags (:standard -warn-error -A))))
